import React from 'react';

export const metadata = {
  title: 'Dashboard - PLStudy',
  description:
    'Productivity website, with curated and personalised statistics.',
};

const layout = ({ children }) => {
  return <>{children}</>;
};

export default layout;
